package com.example.heartsvalentine.hearts.mainSizes;

// This interface is for the global size of image
public interface MainSizes {
    void resetSizes(int newWidth);
    int getWidth();
    int getHeight();
    int getMargin();
}
